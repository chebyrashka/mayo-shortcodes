# Public base URL used when generating stable short links and QR code targets.
# For local development, Astro will infer this from the request when omitted.
PUBLIC_BASE_URL=http://localhost:4321

# Local JSON persistence path. Relative paths are resolved from the project root.
LINK_STORE_PATH=./data/links.json
